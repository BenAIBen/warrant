// docs/config.js — the only file you edit after deploying the backend.
//
// Paste your backend service URL between the quotes below. No trailing slash.
//
//   window.WARRANT_CONFIG = { apiBase: "https://warrant-demo-abc1.onrender.com" };
//
// This is a PUBLIC URL, not a secret. There are no keys, no tokens and no
// credentials anywhere in this project — Warrant talks to one local SQLite file
// and makes no outbound calls, so there is nothing to authenticate to. This
// file is served by GitHub Pages and is world-readable by design. That is fine,
// and it is exactly why nothing secret may ever be put in it.
//
// Until you edit it, the placeholder below is detected and the page shows a
// "not connected to a backend yet" panel instead of making a doomed request.

window.WARRANT_CONFIG = {
  apiBase: "https://<your-app>.onrender.com"   // placeholder — replace with your own
};
